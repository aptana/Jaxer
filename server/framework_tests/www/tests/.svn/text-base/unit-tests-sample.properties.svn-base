#
# Rename to "unit-tests.properties" and update the paths below as is
# appropriate for your local configuration.
#

# Specify the directory where you would like the jam.zip file to be expanded.
# Do not include the trailing slash.
jam-base=E:

# Specify the directory where the framework_tests www directory lives on the
# local drive. Do not include the trailing slash.
unit-test-dir=E:\\aptana\\workspaces-3.3\\jaxer\\framework_tests\\www

# Specify the full path to the local instance of the Firefox executable.
ff=E:\\Program Files\\Mozilla Firefox\\firefox.exe
